// STUB — actioning-prod bundle, prod-standard profile.
// REQUIRES: baseline-hitl + baseline-actions wired for any side_effect: true tool.
targetScope = 'resourceGroup'
param environmentName string
param location string = resourceGroup().location
// TODO: Foundry + AI Search + Container Apps + KV + App Config + App Insights + Action Groups
//       + HITL approval queue + side-effect tool wrappers.
