# actioning-prod-pl — azd template

Private-link variant of `actioning-prod`. The strictest supported bundle.

## Prerequisites
- As `actioning-prod`, plus:
- PE + DNS strategy agreed.
- Restricted tool catalog enforced (see `docs/foundry-tool-catalog.md`).
- Region supports all PE SKUs.
