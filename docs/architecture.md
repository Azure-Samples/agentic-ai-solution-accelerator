# Architecture

Canonical architecture documentation has moved to
[`docs/patterns/architecture/README.md`](patterns/architecture/README.md).

That file now covers the flagship topology, the invariants the lint enforces, the
variants partners can customise into, anti-patterns, out-of-scope items, **and**
the flagship implementation detail (agent graph, Azure topology, data + control
flow, "why this shape") that previously lived here.
